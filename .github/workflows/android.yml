# DEVRE DISI — otomatik build icin android-build.yml kullanilir
name: Build Android APK (legacy)

on:
  workflow_dispatch:

concurrency:
  group: falimabak-android-${{ github.ref }}
  cancel-in-progress: true

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Bu workflow devre disi. Actions > Build Android APK and AAB calistirin."
          exit 1
